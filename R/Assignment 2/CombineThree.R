# Create three vectors with 3 integers each
a <- c(1, 2, 3)
b <- c(4, 5, 6)
c <- c(7, 8, 9)

# Combine vectors into a 3x3 matrix (column-wise)
matrix_data <- cbind(a, b, c)

# Print the matrix
print(matrix_data)